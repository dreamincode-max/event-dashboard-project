# MongoDB
MONGO_URI=your_mongodb_connection_string

# Server
PORT=5000
JWT_SECRET=your_jwt_secret_here

# AI — set ONE or both (OpenAI is tried first, then Gemini, then mock demo mode)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
